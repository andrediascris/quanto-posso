import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ===============================
  // BRAND COLORS
  // ===============================

  /// Azul principal do Quanto Posso
  static const Color primary = Color(0xFF1D1B4F);

  /// Amarelo de destaque / ações principais
  static const Color accent = Color(0xFFFFB020);

  // ===============================
  // BACKGROUND COLORS
  // ===============================

  /// Fundo principal claro
  static const Color backgroundLight = Color(0xFFF8F9FC);

  /// Fundo principal escuro
  static const Color backgroundDark = Color(0xFF12102F);

  // ===============================
  // SURFACE COLORS
  // ===============================

  /// Cards e containers no tema claro
  static const Color surfaceLight = Color(0xFFFFFFFF);

  /// Cards no tema escuro
  static const Color surfaceDark = Color(0xFF242252);

  // ===============================
  // TEXT COLORS
  // ===============================

  static const Color textPrimary = Color(0xFF181735);

  static const Color textSecondary = Color(0xFF7C7A99);

  static const Color textLight = Color(0xFFFFFFFF);

  // ===============================
  // STATUS COLORS
  // ===============================

  /// Gastos dentro do limite
  static const Color success = Color(0xFF2ECC71);

  /// Alertas e erros
  static const Color error = Color(0xFFEF5350);

  /// Avisos
  static const Color warning = Color(0xFFFFB020);

  // ===============================
  // DIVIDERS / BORDERS
  // ===============================

  static const Color border = Color(0xFFE5E5EF);

  static const Color disabled = Color(0xFFB7B6C8);
}
