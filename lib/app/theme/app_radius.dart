class AppRadius {
  AppRadius._();

  // =====================================
  // BORDER RADIUS SYSTEM
  // =====================================

  /// Pequenos elementos:
  /// chips, tags, pequenos containers
  static const double small = 12.0;

  /// Inputs e componentes menores
  static const double medium = 16.0;

  /// Cards principais financeiros
  static const double card = 20.0;

  /// Botões principais
  static const double button = 28.0;

  /// Elementos circulares:
  /// FAB, avatar, indicadores
  static const double circular = 999.0;

  // =====================================
  // SPECIFIC COMPONENTS
  // =====================================

  /// Card de resumo financeiro
  static const double financialCard = 24.0;

  /// Bottom Sheets
  static const double bottomSheet = 28.0;

  /// Dialogs
  static const double dialog = 24.0;
}
